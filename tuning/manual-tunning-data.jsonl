{"prompt":"Show me the fields requestID, arn, recipientAccountId, requestID, webIdFederationData, recipientAccountId, eventType, with a limit of 109 ->","completion":" fields @timestamp, requestID, userIdentity.sessionContext.sessionIssuer.arn, recipientAccountId, requestID, userIdentity.sessionContext.webIdFederationData, recipientAccountId, eventType, @message | sort @timestamp desc ###"}

Who created ?

fields @timestamp,userIdentity.sessionContext.sessionIssuer.userName,eventName,sourceIPAddress,  @message
| filter eventName like "Create"
| sort @timestamp desc
| limit 20

Tell me more about user's activities

fields @timestamp,userIdentity.sessionContext.sessionIssuer.userName,eventName,sourceIPAddress,  @message
| sort @timestamp desc
| limit 20

# Questions:
What
    Fields: 
When
Where
Who
    fields @timestamp,userIdentity.sessionContext.sessionIssuer.userName,eventName,sourceIPAddress 
Show me data 
# actions
    |filter eventName like Random actions
# Conditions
    Random fields Random operation Random Value
    filter
# sort
    Sort random ascending or decending random field

# Limit
